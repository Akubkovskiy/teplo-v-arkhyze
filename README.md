# teplo-v-arkhyze

Публичный сайт-витрина (без админки).

## Принцип
- Админка остаётся в EasyCamp.
- Этот репозиторий содержит только публичный сайт + публичные API-вызовы для витрины.

## Запуск
```bash
cp .env.example .env
docker compose up -d --build
```
